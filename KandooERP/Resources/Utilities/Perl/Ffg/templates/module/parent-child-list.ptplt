# this program template generates a program with parent + child with access to parent data by picking a row in the list screen
# the child and grandchild screen arrays can be included in the main form, else specify the -childformuse
-maintemplate parent-form-scan.mtplt 
-childtemplate child-form-basic.mtplt
-listtemplate list-form-basic.mtplt